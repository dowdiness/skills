# Implementation Plans

Generated by the improve skill on 2026-07-21. Execute in the order below unless
dependencies say otherwise. Each executor must read the selected plan fully,
honor its STOP conditions, preserve the current intentional agent frontmatter
changes, and update the corresponding status row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|---|---|---|---|---|---|
| 001 | Make operational agent prompts evidence-bounded and verifiable | P1 | M | — | DONE (`0350393`, reviewed and approved) |
| 002 | Clarify review roles and make coordinator failures explicit | P1 | M | — | TODO |
| 003 | Add deterministic prompt contracts and reproducible usage evidence | P2 | M | 001, 002 | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) |
REJECTED (with one-line rationale).

## Dependency notes

- Plans 001 and 002 may run in either order because their source-file scopes are
  disjoint except for conceptual consistency.
- Plan 003 depends on the final prompt headings and contracts from Plans 001 and
  002.
- All plans use commit 6336d7b as the pre-change drift baseline. Execute from a checkout containing these plans and the agent frontmatter committed alongside them.

## Findings considered and rejected

- Replace all agents with one generated shared prompt: rejected because role-
  specific constraints are clearer and shared boilerplate would increase prompt
  length for small fallback models.
- Automatically grade semantic correctness in `npm run validate`: rejected
  because model output is nondeterministic, provider-dependent, and potentially
  costly.
- Change `review-router` immediately to decision-only output: rejected for this
  batch because it would break the current invoke-and-return public contract;
  first repair or disprove nested extension wiring with deterministic tests.
- Rewrite `parallel-reviewer` or `skills/parallel-review/SKILL.md`: rejected
  because the current four-reviewer roster and incomplete-review contract are
  aligned.
